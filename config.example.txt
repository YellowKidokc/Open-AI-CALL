# ============================================================
#  Multi-AI Call System — Configuration
# ============================================================
#  COPY this file to  config.txt  and paste your real keys below.
#  Lines starting with # are ignored. You only need keys for the
#  providers you actually want to use — leave the rest blank.
#
#  Keys can ALSO be set as environment variables of the same name,
#  which is handy if you move this folder between machines.
# ============================================================

# ---- API KEYS (fill in the ones you have) ----
OPENAI_API_KEY=sk-PASTE-YOUR-OPENAI-KEY-HERE
ANTHROPIC_API_KEY=sk-PASTE-YOUR-ANTHROPIC-KEY-HERE
DEEPSEEK_API_KEY=
MOONSHOT_API_KEY=
GEMINI_API_KEY=
XAI_API_KEY=

# ============================================================
#  Optional: override the default model for any provider.
#  (Run  python ai_call.py list  to see the defaults.)
# ============================================================
# OPENAI_MODEL=gpt-4o-mini
# ANTHROPIC_MODEL=claude-haiku-4-5
# DEEPSEEK_MODEL=deepseek-chat
# MOONSHOT_MODEL=kimi-k2-0711-preview
# GEMINI_MODEL=gemini-2.0-flash
# XAI_MODEL=grok-2-latest

# ============================================================
#  Optional: response limits (apply to every provider)
# ============================================================
MAX_TOKENS=4096
# TEMPERATURE=0.7        # OpenAI-style providers only; Claude ignores this

# ============================================================
#  Optional behavior
# ============================================================
# KEEP_INBOX=true        # don't move inbox files to process/ after a run
